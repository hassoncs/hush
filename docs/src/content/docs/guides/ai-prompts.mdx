---
title: AI Prompts
description: Copy-paste prompts for the current Hush v3 repository model.
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

These prompts are designed for the current Hush model:

- `.hush/manifest.encrypted`
- `.hush/files/**.encrypted`
- `hush bootstrap`
- `hush config`
- `hush migrate --from v2 [--cleanup]` only when the repo is still legacy

## Quick start prompt

<Aside type="tip" title="Bootstrap the current model">
Use this prompt for a new repo or a repo that already uses `.hush/`.
</Aside>

```markdown
I want to use Hush for AI-native secrets management in this project. Please:

1. Check if sops and age are installed
2. Install @chriscode/hush as a dev dependency
3. Run `npx hush bootstrap`
4. Show me the resulting `.hush/manifest.encrypted` and `.hush/files/**.encrypted` layout with `npx hush config show`
5. Help me add the first secrets with `npx hush set`
6. Verify with `npx hush inspect` and `npx hush config active-identity`

IMPORTANT:
- Do not teach or create `hush.yaml`
- Do not use `hush init` or `hush encrypt`
- If this repo is still legacy v2, stop bootstrapping and use `npx hush migrate --from v2` instead
```

---

## Nested repo bootstrap prompt

<Aside type="tip" title="Child-local repository">
Use this prompt when the project is a git repo nested inside a parent that already has `.hush/`.
</Aside>

```markdown
This git repository is nested inside a parent that has a Hush `.hush/` repo. I need a child-local Hush repository for this project. Please:

1. Run `npx hush bootstrap --new-repo --yes`
2. Run `npx hush doctor` to verify the key resolution
3. Show the resulting layout with `npx hush config show`

Do NOT join the parent `.hush/` repository.
```

---

## Diagnostic prompt

```markdown
Something is wrong with Hush key resolution or root discovery in this project. Please:

1. Run `npx hush doctor` to diagnose the issue
2. If the doctor reports key mismatch, run `npx hush keys setup`
3. Verify with `npx hush inspect`

If bootstrap fails, try `npx hush bootstrap --new-repo --yes` to force a child-local repository.
```

---

## Legacy migration prompt

```markdown
This repository still uses legacy Hush v2 files. Please:

1. Inventory the legacy state
2. Run `npx hush migrate --from v2`
3. Verify the new `.hush/` repository with `npx hush config show` and `npx hush inspect`
4. After validation, run `npx hush migrate --from v2 --cleanup`

Do not keep teaching `hush.yaml`, `hush init`, or `hush encrypt` as the current model after migration.
```

---

## Install the Claude Code skill

<Tabs>
  <TabItem label="Project Skill">
    ```markdown
    Install the Hush skill for this project and verify that it teaches the current v3 workflow:

    - `hush bootstrap`
    - `hush config`
    - `hush set`
    - `hush inspect`
    - `hush run -- <command>`
    - `hush migrate --from v2` only for legacy repos
    ```
  </TabItem>
  <TabItem label="Personal Skill">
    ```markdown
    Install the Hush skill globally and verify it points to the current `.hush/` repository model instead of legacy `hush.yaml` docs.
    ```
  </TabItem>
</Tabs>

---

## Common task prompts

### Check what secrets exist

```markdown
Use Hush to show me what secrets are configured in this project.
Use `hush inspect` and `hush config show`.
Do not read plaintext env files directly.
```

### Add a new secret

```markdown
Help me add a new secret called [SECRET_NAME].
Use `hush set` and then verify with `hush inspect`.
Do not create or edit plaintext env files.
```

### Inspect the current repository state

```markdown
Show me the current Hush v3 repository state.
Use:
- `hush config show`
- `hush config active-identity`
- `hush config show files`
```

### Configure a monorepo target

```markdown
Help me inspect and update my Hush v3 monorepo setup.
Use `hush config` to show the current targets, files, and readers.
Keep the canonical state in `.hush/manifest.encrypted` and `.hush/files/**.encrypted`.
```

---

## Cursor integration

Add this to your `.cursorrules` file:

```markdown
## Secrets Management

This project uses Hush v3.

Current model:
- `.hush/manifest.encrypted`
- `.hush/files/**.encrypted`

Use these commands:
- `npx hush config show`
- `npx hush inspect`
- `npx hush has <KEY>`
- `npx hush set <KEY>`
- `npx hush run -- <command>`

Legacy bridge only:
- `npx hush migrate --from v2`
- `npx hush migrate --from v2 --cleanup`

Do not teach or recreate `hush.yaml`, `hush init`, or `hush encrypt` as the current setup.
```

---

## Troubleshooting prompt

```markdown
Hush isn't working in this project. Please diagnose:

1. Check whether the repo already uses `.hush/`
2. Run `npx hush config show`
3. Run `npx hush inspect`
4. Verify sops and age are installed
5. If the repo is legacy v2, use `npx hush migrate --from v2`

Do not switch back to `hush.yaml`, `hush init`, or `hush encrypt` as the main path.
```

<Aside type="note" title="Why these prompts work">
These prompts keep the assistant on the current `.hush/` repository model and reserve `hush migrate --from v2` for older repositories only.
</Aside>
