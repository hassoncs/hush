# Migration Learning: hq-ch5

**Date:** 2026-04-21
**Project:** hassoncs/hq-ch5
**Path:** `/Users/hassoncs/Workspaces/Personal/platform/hq-ch5`
**Result:** Success — full v2 → v3 migration

## Pre-migration state

- `hush.yaml` present (v2 legacy config)
- `.sops.yaml` present with age public key
- 1 encrypted source file: `.hush.encrypted` (shared env)
- Missing: `.hush.development.encrypted`, `.hush.production.encrypted`, `.hush.local.encrypted`
- No `.hush/` directory (not bootstrapped)
- Age key available locally: `~/.config/sops/age/keys/hassoncs-hq-ch5.txt`

## Path taken

`hush migrate --from v2` (not bootstrap — encrypted file existed).

Dry-run showed 1/4 source files present, 1 target (`root`), 2 repo refs to review.

Migration succeeded in one pass. Cleanup (`--migrate --from v2 --cleanup`) removed `hush.yaml`, `.hush.encrypted`, and `migration-v2-state.json`.

## Post-migration state

- `.hush/manifest.encrypted` — present, readable (14 secrets across `env/project/shared`, `env/targets/root/production`, `env/targets/root/runtime`)
- `.hush/files/env/` — 5 encrypted files, 0 unreadable
- `hush status`: v3, 3 identities, 3 bundles, 2 targets
- `hush inspect`: all entries readable, no gaps
- `.gitignore`: `.hush` entry removed by migrate; `.hush-materialized/` kept; `.hush.local` kept

## Hazards encountered

- **SOPS_AGE_KEY_FILE not set in shell** — `hush status` showed decryption failure without the env var. Required `SOPS_AGE_KEY_FILE=~/.config/sops/age/keys/hassoncs-hq-ch5.txt` explicitly. Not a migration blocker but worth noting for runtime usage docs.
- **Plaintext `.dev.vars`** at `apps/deploy/.dev.vars` with real-looking secret values. File was gitignored (`.gitignore` had `.dev.vars`). Not committed, no action required, but the secrets it contains (`FITBOT_CI_CLIENT_SECRET`, `GITHUB_TOKEN`, etc.) are now managed in `.hush/` encrypted files under the shared bundle.

## No blockers, no stale scripts

No `hush decrypt` usage found in package.json scripts or any source files.

## Key lessons

- Single encrypted source file + present age key = straightforward `migrate` path (not bootstrap)
- Bare `.hush` gitignore line correctly removed by migrate — no manual fix needed
- Always set `SOPS_AGE_KEY_FILE` when running hush commands in fresh shell sessions for this project
