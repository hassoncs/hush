# Migration learning: hassoncs/devbox — 2026-04-21

## Project

`/Users/hassoncs/Workspaces/Personal/dev/devbox`

## Starting state

- `hush.yaml` v2, `version: 2`, project `hassoncs/devbox`
- `.hush.encrypted` present (1 encrypted file — shared env)
- `.sops.yaml` with age public key
- `.gitignore` contained bare `.hush` entry (would block v3 commits)
- Sources declared: shared, development, production, local — only shared had an encrypted file

## Migration path

Used `hush migrate --from v2` (not bootstrap) — encrypted file existed, so migrate was correct.

```
hush migrate --from v2 --dry-run   # confirmed 1/4 sources present
hush migrate --from v2             # ran migration
hush migrate --from v2 --cleanup   # removed hush.yaml, .hush.encrypted, migration-v2-state.json
```

## Key handling

Key `hassoncs/devbox` was present at `~/.config/sops/age/keys/hassoncs-devbox.txt` but not auto-loaded in shell. Had to set `SOPS_AGE_KEY_FILE` explicitly for status/inspect to work. In normal interactive shells this is presumably handled by direnv or shell config.

## Post-migration state

- `.hush/` created with `manifest.encrypted` + 5 encrypted files
- v3 status: 5 readable, 0 unreadable, identities=3, bundles=3, targets=2
- Secrets intact: `HCLOUD_TOKEN`, `RUNPOD_API_KEY` (shared + projected into root target)
- `.gitignore`: bare `.hush` removed automatically by migrate; `.hush-materialized/` added

## Cleanup done

- `hush.yaml` removed
- `.hush.encrypted` removed
- README.md updated: stale `hush.yaml / .hush.encrypted` reference replaced with `.hush/` v3 description

## Notes

- No blockers; single encrypted source file migrated cleanly
- `migrate --from v2` auto-fixed the `.gitignore` bare `.hush` hazard — no manual intervention needed
- Only stale artifact was a README line, no script/CI hush decrypt usage found
